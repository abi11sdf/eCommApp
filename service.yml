apiVersion: v1
kind: Service
metadata:
  name: ecom-try-service
  labels:
    app: ecom-try
spec:
  type: LoadBalancer
  selector:
    app: ecom-try
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
